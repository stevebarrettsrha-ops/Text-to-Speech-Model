{
  "name": "script-builder-tts",
  "version": "1.0.0",
  "private": true,
  "description": "Script Builder — multi-speaker text-to-speech web app (static site, deployed to GitHub Pages)",
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node tests/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
